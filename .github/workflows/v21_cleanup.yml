name: V21 cleanup and warning-free validation

on:
  push:
    branches: [v21]
    paths:
      - '.github/workflows/v21_cleanup.yml'

permissions:
  contents: write

jobs:
  cleanup-test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v21
          fetch-depth: 0

      - name: Prepare warning-free source
        run: |
          cat > .gitignore <<'EOF'
          __pycache__/
          *.py[cod]
          .pytest_cache/
          .pio/
          EOF
          python3 - <<'PY'
          from pathlib import Path
          p = Path('Src/vesc_protocol.c')
          s = p.read_text()
          old = '''static float angle_diff_deg(float a, float b)
          {
              float d = a - b;
              while (d > 180.0f) d -= 360.0f;
              while (d < -180.0f) d += 360.0f;
              return d;
          }

          '''
          if old in s:
              p.write_text(s.replace(old, '', 1))
          PY

      - name: Install test/build dependencies
        run: python3 -m pip install --disable-pip-version-check pytest platformio

      - name: Regression suite
        run: python3 -m pytest -q

      - name: Warning-free STM32 build
        run: |
          set -o pipefail
          pio run -e esc 2>&1 | tee /tmp/v21_build.log
          if grep -E '(^|: )[Ww]arning:' /tmp/v21_build.log; then
            echo 'Compiler warning detected; refusing V21 cleanup commit.'
            exit 1
          fi

      - name: Remove generated artifacts from release diff
        run: |
          rm -rf tests/__pycache__ .pytest_cache
          # V20 tracked .pio output. Restore it byte-for-byte so V21 does not
          # contain irrelevant compiler-generated binary diffs.
          git checkout origin/v20 -- .pio

      - name: Commit cleanup
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then
            echo 'No cleanup changes.'
          else
            git commit -m 'V21: exclude generated artifacts from release diff'
            git push origin HEAD:v21
          fi
